const Directions = require('directions');

let HorizontalDirections = cc.Enum({
    NONE: Directions.NONE,
    EAST: Directions.EAST,
    WEST: Directions.WEST
});

export default HorizontalDirections;
